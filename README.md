# GXFB

Experiment trying to use GX for an accelerated RGB framebuffer on the Nintendo GameCube/Wii.
2 versions are available:
* `efb/`: Directly using the EFB
* `texture/`: Using a texture then rendering that texture
